/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const picturesAPI_auto = {

    /**
    * Gets all Pictures
    */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/pictures`,requestOptions);
        return response.data;
    },

    /**
    * Gets an entry from 'Pictures' by its primary key
    */
    getById: async function(pictureId) {
        let response = await axios.get(`${BASE_URL}/pictures/${pictureId}`,requestOptions);
        return response.data[0];
    },

    /**
    * Creates a new entry in 'Pictures'
    */
    create: async function(formData, ) {
        let response = await axios.post(`${BASE_URL}/pictures`,formData, requestOptions);
        return response.data;
    },

    /**
    * Updates an existing entry in 'Pictures' by its primary key
    */
    update: async function(formData, pictureId) {
        let response = await axios.put(`${BASE_URL}/pictures/${pictureId}`,formData, requestOptions);
        return response.data;
    },

    /**
    * Deletes an existing entry in 'Pictures' by its primary key
    */
    delete: async function(pictureId) {
        let response = await axios.delete(`${BASE_URL}/pictures/${pictureId}`,requestOptions);
        return response.data;
    },
};

export {picturesAPI_auto};