/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const aficionesAPI_auto = {

    /**
    * Gets all aficiones
    */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/aficiones`,requestOptions);
        return response.data;
    },

    /**
    * Gets an entry from 'aficiones' by its primary key
    */
    getById: async function(aficionID) {
        let response = await axios.get(`${BASE_URL}/aficiones/${aficionID}`,requestOptions);
        return response.data[0];
    },

    /**
    * Creates a new entry in 'aficiones'
    */
    create: async function(formData, ) {
        let response = await axios.post(`${BASE_URL}/aficiones`,formData, requestOptions);
        return response.data;
    },

    /**
    * Updates an existing entry in 'aficiones' by its primary key
    */
    update: async function(formData, aficionID) {
        let response = await axios.put(`${BASE_URL}/aficiones/${aficionID}`,formData, requestOptions);
        return response.data;
    },

    /**
    * Deletes an existing entry in 'aficiones' by its primary key
    */
    delete: async function(aficionID) {
        let response = await axios.delete(`${BASE_URL}/aficiones/${aficionID}`,requestOptions);
        return response.data;
    },
};

export {aficionesAPI_auto};